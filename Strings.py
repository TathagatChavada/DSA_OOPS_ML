str = "python programming"

# Returns a copy of string with its first character capitalized
# print(str.capitalize())

# Returns the number of occurences of the substring
# print(str.count('on'))

# Returns the lowest index in the string where the substring is found
# print(str.find('py'))

# Returns lowest index where specified substring is found
# print(str.index('th'))

# Returns the title cased version of string
# print(str.title())

# Returns a copy of string with all occurences replace by new string
# print(str.replace("python","C++"))

# Join each character with string based iterator
# print("*".join(str))

# Splits string into list
print(str.split())